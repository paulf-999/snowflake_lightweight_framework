{
    "Parameters" : {
        "Program" : "EG",
        "Environment" : "np",
        "Stage" : "np",
        "Branch" : "develop",
        "AwsAccountId": 123456789199,
        "SnowflakeParameters" : {
            "SnowflakeVPCID" : "vpc-999f12a1",
            "SnowflakeIAMRoleName" : "${PROGRAM_LOWER}-snowflake-access-role", 
            "SnowSqlQueryTemplate": "snowsql -c ${SNOWFLAKE_CONN_PROFILE} -o friendly=false -o header=false -o timing=false"
        },
        "AdditionalParameters" : {
            "S3Bucket": "${PROGRAM}-${DATA_SRC}-raw-${AWS_ACCOUNT_ID}-${STAGE}-${BRANCH}"
        }
    }
}
